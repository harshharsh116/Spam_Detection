# Spam_Detection
A machine learning-based application that classifies SMS messages as Spam or Ham (Not Spam). The project uses text preprocessing, Bag of Words (CountVectorizer), and Logistic Regression to convert SMS text into numerical features and predict whether a message is spam.
